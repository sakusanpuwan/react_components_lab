import React from "react"

const NavBar = () => {

    return (

    <div>
        <nav>
            <a href="#">Home</a>
            <a href="#">Recipes</a>
            <a href="#">Submit</a>
            <a href="#">Contact</a>
        </nav>
    </div>
    );

};

export default NavBar;